import { CreateAppointmentDto } from './create-appointment-dto';

describe('CreateAppointmentDto', () => {
  it('should create an instance', () => {
    expect(new CreateAppointmentDto()).toBeTruthy();
  });
});
