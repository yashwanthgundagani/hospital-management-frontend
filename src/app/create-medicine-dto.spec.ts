import { CreateMedicineDto } from './create-medicine-dto';

describe('CreateMedicineDto', () => {
  it('should create an instance', () => {
    expect(new CreateMedicineDto()).toBeTruthy();
  });
});
